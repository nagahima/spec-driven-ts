/**
 * エントリポイント(スタブ)。実装はdocs/implementation-plan.mdのPhase順で行う。
 * サブコマンド:
 *   report generate --companies config/companies.json
 *   tools snapshot   (ADR-0008: contracts/edinetdb.snapshot.json を更新)
 */
console.error("Not implemented yet. See docs/implementation-plan.md");
process.exit(1);
